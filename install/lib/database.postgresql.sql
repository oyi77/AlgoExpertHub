-- PostgreSQL Database Schema
-- NOTE: This file needs to be converted from MySQL schema
-- Key differences:
-- - AUTO_INCREMENT -> SERIAL or GENERATED ALWAYS AS IDENTITY
-- - Backticks ` -> Double quotes "
-- - ENGINE=InnoDB -> Remove (PostgreSQL uses tablespaces)
-- - UNSIGNED -> Remove
-- - TINYINT(1) -> BOOLEAN or SMALLINT
-- - DATETIME -> TIMESTAMP

-- For now, this is a placeholder. The installer will attempt to use
-- database.mysql.sql if this file doesn't exist or is incomplete.
-- A full PostgreSQL conversion should be done manually or via conversion tool.

-- Example structure (needs full conversion):
-- CREATE TABLE sp_admins (
--   id BIGSERIAL PRIMARY KEY,
--   username VARCHAR(255) NOT NULL,
--   email VARCHAR(255) NOT NULL,
--   ...
-- );

