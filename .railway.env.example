# Railway Environment Variables Example
# Copy these to Railway Dashboard → Service → Variables
# Railway automatically provides MYSQL* variables when MySQL service is linked

# ==========================================
# APPLICATION CONFIGURATION
# ==========================================
APP_NAME=AlgoExpertHub
APP_ENV=production
APP_DEBUG=false
APP_KEY=base64:YOUR_GENERATED_KEY_HERE
APP_URL=https://your-app.up.railway.app
APP_BASE_PATH=main

# ==========================================
# DATABASE (Auto-provided by Railway MySQL)
# ==========================================
DB_CONNECTION=mysql
DB_HOST=${MYSQLHOST}
DB_PORT=${MYSQLPORT}
DB_DATABASE=${MYSQLDATABASE}
DB_USERNAME=${MYSQLUSER}
DB_PASSWORD=${MYSQLPASSWORD}

# ==========================================
# QUEUE CONFIGURATION
# ==========================================
QUEUE_CONNECTION=database
# Or use Redis: QUEUE_CONNECTION=redis

# ==========================================
# CACHE CONFIGURATION
# ==========================================
CACHE_DRIVER=file
# Or use Redis: CACHE_DRIVER=redis

# ==========================================
# SESSION CONFIGURATION
# ==========================================
SESSION_DRIVER=database
SESSION_LIFETIME=120
# Or use Redis: SESSION_DRIVER=redis

# ==========================================
# LOGGING
# ==========================================
LOG_CHANNEL=stack
LOG_LEVEL=info

# ==========================================
# REDIS (If using Redis service)
# ==========================================
# REDIS_HOST=${REDISHOST}
# REDIS_PORT=${REDISPORT}
# REDIS_PASSWORD=${REDISPASSWORD}

# ==========================================
# MAIL CONFIGURATION
# ==========================================
MAIL_MAILER=smtp
MAIL_HOST=smtp.mailtrap.io
MAIL_PORT=2525
MAIL_USERNAME=your_mail_username
MAIL_PASSWORD=your_mail_password
MAIL_ENCRYPTION=tls
MAIL_FROM_ADDRESS=noreply@yourdomain.com
MAIL_FROM_NAME="${APP_NAME}"

# ==========================================
# TELEGRAM BOT (If using)
# ==========================================
TELEGRAM_BOT_TOKEN=your_bot_token

# ==========================================
# AI SERVICES (If using)
# ==========================================
OPENAI_API_KEY=your_openai_api_key
GEMINI_API_KEY=your_gemini_api_key
OPENROUTER_API_KEY=your_openrouter_api_key

# ==========================================
# AWS S3 (If using S3 for file storage)
# ==========================================
# AWS_ACCESS_KEY_ID=your_aws_key
# AWS_SECRET_ACCESS_KEY=your_aws_secret
# AWS_DEFAULT_REGION=us-east-1
# AWS_BUCKET=your_bucket_name
# FILESYSTEM_DRIVER=s3

# ==========================================
# PAYMENT GATEWAYS (Configure via Admin Panel)
# ==========================================
# Payment gateway credentials should be configured via Admin Panel
# No env vars needed here
