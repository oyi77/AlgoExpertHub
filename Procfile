# Railway Procfile (alternative to start commands)
# Railway can use this format or service-specific start commands

# Web service (main application)
web: bash railway-deploy.sh

# Worker service (queue processing)
worker: bash railway-worker.sh

# Scheduler service (cron replacement)
scheduler: bash railway-scheduler.sh
